services:
  nginx:
    image: nginx:alpine
    network_mode: host
    volumes:
      - ./poker.conf:/etc/nginx/conf.d/default.conf:ro
      - ${CERTS_PATH}:/etc/nginx/certs:ro
      - ../web-export:/usr/share/nginx/html:ro
    restart: unless-stopped
