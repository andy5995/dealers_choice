services:
  nginx:
    image: nginx:alpine
    network_mode: host
    volumes:
      - ./nginx/poker.conf:/etc/nginx/conf.d/default.conf:ro
      - ./web-export:/usr/share/nginx/html:ro
    restart: unless-stopped
